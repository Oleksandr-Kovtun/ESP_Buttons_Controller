# ESP_Buttons_Controller
🔘 ESP32 OSC & Serial Controller (8 Buttons)
  Це професійна прошивка для ESP32, яка перетворює контролер на потужний інструмент для медіа-художників та VJ. Вона дозволяє керувати софтом (Isadora, TouchDesigner, Resolume) через OSC (WiFi) та Serial (USB) одночасно.

🚀 Основні можливості
  8 фізичних кнопок: Підтримка до 8 входів з антибрязкітом (debounce).
  Web-інтерфейс: Зміна всіх налаштувань через браузер (192.168.4.1).
  Динамічний OSC: Можливість задати унікальний OSC-шлях для кожної кнопки.
  Гнучкий "Alive" сигнал: Трансляція статусу та IP-адреси в Serial для моніторингу зв'язку.
  Енергонезалежна пам'ять: Всі налаштування зберігаються у файловій системі LittleFS.
  Подвійний режим WiFi: Одночасна робота як точки доступу (AP) для налаштування та клієнта (STA) для роботи в мережі.

🛠 Технічні деталі
  Підключення кнопок (Pins)
  Кнопка    Пін ESP32    Кнопка    Пін ESP32
  Button 1  GPIO 13      Button 5  GPIO 26
  Button 2  GPIO 12      Button 6  GPIO 25
  Button 3  GPIO 14      Button 7  GPIO 33
  Button 4  GPIO 27      Button 8  GPIO 32

Примітка: У поточній версії використовується режим INPUT. Переконайтеся, що ваші кнопки мають зовнішні підтягуючі резистори.

Вихідні дані
  Serial (115200 baud):
    Формат натискання: b1/1 (натиснуто), b1/0 (відпущено).
    Символ закінчення рядка: CR (ASCII 13).
    Alive: alive | IP: 192.168.x.x.
  OSC (UDP):
    Надсилає ціле число 1 або 0 на задану адресу (наприклад, /b1).
    
⚙️ Налаштування
  Перший запуск: Після прошивки ESP32 створить WiFi мережу ESP32_OSC_CONFIG.
  Веб-панель: Підключіться до цієї мережі та перейдіть у браузері за адресою http://192.168.4.1.
  Конфігурація:
    Введіть дані вашого WiFi (SSID/Pass).
    Вкажіть Target IP (IP вашого комп'ютера з Isadora/TouchDesigner).
    Задайте назви OSC команд для кожної кнопки.
    Налаштуйте інтервал "Alive" повідомлень.
  Збереження: Натисніть "SAVE & RESTART". ESP32 збереже дані та спробує підключитися до вашої мережі.
  
💻 Необхідні бібліотеки
  Для компіляції в Arduino IDE або Arduino Cloud встановіть:
    WiFi (вбудована)
    WiFiUdp (вбудована)
    OSC (by Adrian Freed, Yotam Mann)
    ESPAsyncWebServer
    AsyncTCP
    ArduinoJson (v7.x)
    LittleFS (вбудована)
    
⌨️ Команди Serial (через монітор порту)
  alive on / alive off — керування фоновим сигналом.
  reboot — дистанційне перезавантаження контролера.
